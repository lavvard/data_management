CREATE TABLE shop_DNS(
	component_id int PRIMARY KEY,
	component_name varchar(220),
	component_type varchar(100),
	manufacturer varchar(220),
	model_number varchar(100)
);
CREATE TABLE shop_MVideo(
	component_id int PRIMARY KEY,
	component_name varchar(220),
	component_type varchar(100),
	manufacturer varchar(220),
	model_number varchar(100)
);
CREATE TABLE shop_CityLink(
	component_id int PRIMARY KEY,
	component_name varchar(220),
	component_type varchar(100),
	manufacturer varchar(220),
	model_number varchar(100)
);

